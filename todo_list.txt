Go through all the API calls and properly handle errors.
Tool listing, probably a card view ordered by proximity.  Also on a map?
Some kind of free search thing on the tool list to filter it.
In manage friends, show how many tools each one has.
Eventually, some AI thing that does "if I want to tile my shower, what tools do I need to borrow"
